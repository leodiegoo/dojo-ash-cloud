const { addAeroportos } = require('./addAeroportos');
const { addNuvens } = require('./addNuvens');
const { criarMapa } = require('./criarMapa');
const { resolverProblema } = require('./problema');

module.exports = { addAeroportos, addNuvens, criarMapa, resolverProblema };
